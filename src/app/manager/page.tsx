import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function ManagerPage() {
  return (
    <PlaceholderPage
      eyebrow="Cleaning manager"
      title="Manager workspace"
      description="Approve proposed cleans, assign cleaners, and review linen and long-clean reports."
      nextAction="Job approval and assignment workflows will be implemented after the data model is defined."
    />
  );
}
